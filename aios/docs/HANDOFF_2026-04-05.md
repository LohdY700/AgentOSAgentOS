# AIOS — Handoff (2026-04-05)

## Done today

1. **LangChain-first memory backend shipped**
   - Added configurable memory backend (`config/memory-backend.json`).
   - Runtime behavior: request `langchain`, auto-fallback to `local` when unavailable.

2. **Memory backend reliability fixes**
   - Fixed import path issue causing unintended fallback.
   - Added backend handle caching to avoid repeated warmup/re-init overhead.
   - Migrated embeddings import toward `langchain-huggingface` (with backward fallback).

3. **Tests stabilized and passing**
   - Updated fallback test to force LangChain failure deterministically (environment-independent).
   - Full test suite passing: **20/20**.

4. **Doctor now validates memory layer**
   - Added checks:
     - `memory_backend` (requested/active/fallback/note)
     - `memory_rw` (real add/search probe)
   - Verified doctor returns `ok=true` with memory checks passing.

5. **Packaging + CI improvements**
   - Added optional deps group: `.[memory]` in `pyproject.toml`.
   - Added CI job `memory-profile`:
     - install `.[memory]`
     - run full tests
     - run doctor (includes memory checks)

6. **Docs & release hygiene**
   - Added README CI badge + CI Profiles section.
   - Added release notes: `docs/RELEASE_NOTES_v0.1.4.md`.
   - Created and pushed tag: **`v0.1.4`**.

---

## Current known status

- Dashboard memory status verified:
  - `requested=langchain`
  - `active=langchain`
  - `fallback_used=false`
- Clean-venv smoke install and doctor check: **PASS**.

---

## Next 3 tasks (priority)

1. **Add API integration tests for memory endpoints**
   - Cover `/api/memory/add` and `/api/memory/search` end-to-end via dashboard server.

2. **Reduce HF warmup/log noise for production**
   - Add optional env docs + cleaner startup behavior/log level guidance.

3. **Prepare publish bundle**
   - Final short launch posts (VN/EN) + one screenshot flow from dashboard.

---

## Quick resume commands

```bash
cd aios
make test
PYTHONPATH=src .venv/bin/python -m aios_core.cli doctor
make dashboard
```
